javascript:l=prompt("what do you want the max to be? ex: 1-6 max:7");m=prompt("what do you want the min to be? ex: 1-6 min=0");function getRandomNumberBetween(min,max){ return Math.floor(Math.random()*(max-min+1)+min); };a=getRandomNumberBetween(0,21)
