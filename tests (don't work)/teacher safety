var safety=prompt("what tab do you want to open when a teacher comes by? click escape to go to that tab. warning: may have to click out of element for it to work");

window.addEventListener("keydown",function(event){
  if (event.key=="s"){
    window.open(safety);
    window.close();
  }
});
