javascript:function get_cookie(cookie_name) { const value = "; " + document.cookie; const parts = value.split("; " + cookie_name + "="); if (parts.length === 2) return parts.pop().split(";").shift(); } var getcookievalue = get_cookie("notes"); var note=prompt("- notes - do not press cancel - only works if you use on same website -",getcookievalue); document.cookie="notes="+note+"; expires=Thu, 18 Dec 9013 12:00:00 UTC"; window.location.reload(1);
